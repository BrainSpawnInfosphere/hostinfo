language: python
sudo: false
python:
  - "2.7"
# command to install dependencies
install:
    - pip install -U pip setuptools wheel
    - pip install psutil simplejson simplehtml py-cpuinfo
# command to run tests
# script: nosetests -v test.py
script: True
